<UserControl x:Class="MusicRecognitionProject.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MusicRecognitionProject.Views"
             xmlns:icons="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Style="{StaticResource MahApps.Styles.Button.Circle}" Width="700" Height="700" Background="Transparent"
                VerticalAlignment="Center" HorizontalAlignment="Center" Click="OnButtonClick">
            <icons:PackIconPhosphorIcons Kind="VinylRecordLight" Width="700" Height="700"/>
        </Button>
        <Rectangle Height="3" Margin="15,0" Fill="LightGray" VerticalAlignment="Top" Grid.Row="1"/>

    </Grid>
</UserControl>
